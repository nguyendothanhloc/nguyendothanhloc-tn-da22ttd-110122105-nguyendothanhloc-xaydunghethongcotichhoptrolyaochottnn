import './bootstrap';

// Import Bootstrap
import 'bootstrap';

// Import jQuery
import $ from 'jquery';
window.$ = window.jQuery = $;

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();
